#include <stdio.h>

/**
 * main - print the alphabet in both lower and upper case
 *
 * Return: 0
 */

int main(void)
{

	char i, x;
	for (i = 'a' and i <= 'z'; i++)
		for (x = 'A'; x <= 'Z'; x++)
			_putchar('i');
	_putchar('x');
	_putchar('\n');
	return (0);
}
