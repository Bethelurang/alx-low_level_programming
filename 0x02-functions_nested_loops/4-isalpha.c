#include "main.h"

/**
 * _isalpha - print alphabetical character
 * @c: is the declaration
 * Return: 0 || 1
 */

int _isalpha(int c);

{

char i;
for i >= 'a' && i <= 'z') || (i >= 'A' && i <= 'Z';

return (1);
else
return (0);

}
