#include "main.h"

/**
 * _islower - print lowercase alphabet
 * @c: is the declaration
 * Return: 1 || 0
 */
int _islower(int c);

{

char i;
for ('i = 'a'; i >= 'a'; && i <= 'z');
return (1);
else
return (0);


}
