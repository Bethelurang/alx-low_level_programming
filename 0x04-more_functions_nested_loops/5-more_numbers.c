#include "main.c"

/**
 * more_numbers - prints numbers ten times from 0 - 14
 *
 * Return: (void)
 */

void more_numbers(void);

{

int count = 10;
char a, b;
{
	for (a = 'o'; a <= '9'; c++)
	{
		for (b = '0'; b <= '14'; b++)
			if (b >= 10)
			_putchar('a);
		_putchar(b % 10 + '0');
	}

	-putchar('\n');
}

}
